# dsh-peer-sessions · 维护手册

> **接手一个新对话时，先读本文件。** 它假设你对本项目一无所知，并且不依赖任何历史对话。
> 本文件随仓库公开，已做过敏感信息检查。
> `docs/design.md` 是**唯一权威需求规格**；本文件讲的是"怎么继续做"，不是"应该做成什么样"。
>
> **本文件是公开的，所以刻意不含机器相关的标识。** 若同目录下存在被 gitignore 的
> `MAINTENANCE-NOTES.md`（本机专用：推送配方、账号、路径），**读它**——接手维护通常两件都要。
>
> 当前状态：**M1 已实现并逐条真机验证；M2 进行中**——第 1+2 项已实现并已真机验证（见第十一节末）。

---

## 一、项目是什么

一句话：**让同一个人开着的两个平级对话能互相通信** —— 没有父会话，没有子代理。

与 `subagent` 的分界线是唯一需要记住的判据：

> **一方结束/失败是否意味着另一方也该停？** 是 → subagent。否 → peer。

现有的 `send_message` / `list_agents` 建立在 `isOwnedBy(parent, child)` 的所有权校验上，而 peer 的前提恰恰是**没有所有权**，所以不能复用——这是本项目存在的原因。

## 二、文件地图

| 路径 | 作用 |
|---|---|
| `lib/index.js` | 插件入口：声明注入服务、装配两半、跟随语言变化重新注册命令 |
| `lib/addressable.js` | **H1**：侧栏一致的可寻址集合（**唯一**列表来源）、输入净化 |
| `lib/messages.js` | **H2**：来源不可伪造的消息构造 |
| `lib/consent.js` | **H3**：插件直接向人取授权（一张卡覆盖整个决定） |
| `lib/core.js` | 投递主路径、进度渲染、拒绝语义 |
| `lib/store.js` | 通道、授权档位、额度、收件箱（**进程内**，见坑 1） |
| `lib/commands.js` / `lib/tools.js` | 2 条命令 + 4 个工具 |
| `lib/i18n.js` | zh/en 文案表、语言解析、插值 |
| `test/peer.test.js` | 79 项测试（需要依赖链接，见第四节） |
| `skill/peer-session/SKILL.md` | 给**模型**看的说明书（安装时复制到 `$DSH_HOME/skills/`） |
| `docs/design.md` | 权威需求规格 + 真机验证表（§12） |
| `scripts/link-deps.sh` | 把本包 `node_modules` 链到 harness 部署 |

## 三、它是怎么接进去的（三件事，缺一不可）

1. **依赖链接** —— `npm run link`
2. **profile 依赖 + 补丁行** —— `~/.dsh/profiles/web/package.json` 里的 `link:` 依赖，以及 `~/.dsh/profiles/web/cordis.patch.yml` 里的一行 `insert`
3. **复制技能** —— `cp -r skill/peer-session ~/.dsh/skills/`

一键安装：`./install.sh`（改补丁层前**自动备份**并打印回滚命令）。卸载：`./uninstall.sh`。

## 四、开发与测试

```sh
npm run link     # 一次性，指向本机已安装的 harness
npm test         # 79 项，不需要启动 harness
```

**为什么没有 CI**：测试会 `import '@deepseek-ai/dsh-llm'`，用**真实的** `createUserMessage` 验证消息构造，而那个包来自本机已安装的 harness 部署。干净的 clone 里没有它，CI 跑不起来。

**这不是遗漏，是这类插件的固有约束**——而且是有意保留的：把消息构造换成自己造的假实现，就会丢掉它曾经抓到的东西（见坑 6）。真机验证只能人工做（第八节）。

## 五、四条硬不变量

破坏任何一条都是严重缺陷。完整陈述与推论见 `docs/design.md` §5。

| | 内容 | 落点 |
|---|---|---|
| **H1** | agent 眼里的对话列表 ⊆ 人在侧栏能看到的列表。**绝不能对一个人类看不见的对话说话。** | `addressable.js` |
| **H2** | peer 消息可归属，且**永不可伪装成用户指令**。**绝不使用 `sessionController.prompt()`**（它写入 `source:{kind:'user'}`） | `messages.js` |
| **H3** | 授权由插件**直接**向人取得，模型不经手、无法转述、无法篡改档位 | `consent.js` |
| **H4** | peer 消息不升级任何权限：不能批准、不能改权限、不能唤醒冷会话之外的副作用 | `core.js` / `tools.js` |

## 六、已经踩过的坑（新对话最该读的一节）

### 1. 加载器 re-apply 插件，但**不重新求值模块**

包文件一变化，加载器就重新 `apply()` 这个插件——但它是重新 import **同一份被缓存**的模块。两个后果，都很容易误判：

- **改代码不会生效**，除非重启进程。改了文件以为"测试一下"，其实跑的还是旧代码。
- **re-apply 会执行 `apply()`**，所以任何在 `apply()` 里创建的状态都会被丢弃。

`store` 因此放在**模块作用域**（见 `lib/index.js` 的注释）。若 store 建在 `apply()` 里，一次 re-apply 就会静默清空所有已开通道——真机上观测到过。

### 2. 技能必须**复制**，不能软链

技能提供者用 `lstat` 语义列举技能根（`dsh-fs-local`），**软链目录会被判成 `other` 而不是目录**，技能永远不会被发现。`install.sh` 用的是 `cp -R`。

### 3. 本包的 `node_modules` 必须是**软链**，不能是真目录

Node 按导入文件的 **realpath** 解析裸说明符。`link:` 安装把本包留在 profile 树之外，所以 `@deepseek-ai/dsh-llm` / `dsh-tools` 解析不到。链到 `$DSH_HOME/profiles/node_modules` 既恢复解析，又让它指向**与宿主同一份 realpath**——同一模块实例，没有第二份副本。

**不要改成普通 npm 依赖**：那会在插件与宿主之间制造身份分裂（两份定义了同一类型的包）。

### 4. `.gitignore` 里 `node_modules/`（带斜杠）**匹配不到软链**

尾部斜杠只匹配**目录**。软链不是目录，所以它会**被提交进仓库**。必须同时有裸写的一行 `node_modules`。

### 5. 工具是**全局注册**的，所以子代理也看得到

没有任何 preset 配置 `toolFilter`，委派工具因此不裁剪子代理的工具面——探针子代理报告 4 个 `peer_*` 工具**全部可见**。

**本插件的子代理安全性来自代码守卫（`isRuntimeRoot`），不是来自"工具不存在"。** 那个守卫是承重件。真机已验证：子代理看到工具、调用被拒、**且没有弹出任何卡片**（`userQuestions.ask` 从未被调用，因此不会出现契约警告的"被拥有的子代理永久阻塞"）。

### 6. 投影值是 `JsonValue` —— **键存在不等于值是对象**

`projections.values.title` 对所有尚无标题的会话就是 `null`。只判 `!== undefined` 就去读属性会崩。用 `addressable.js` 的 `asRecord()`。

同一个坑还有第二层：**真实形状与直觉不符**，而且我全都猜错过一次。

| 投影 | 直觉（错） | 真实 |
|---|---|---|
| `todos` | `{ items: [...] }`，键名 `todo` | **数组** `TodoItem[] \| null`，键名 **`todos`** |
| `goal` | `goal.objective` | `goal.goal.objective`（快照多嵌一层）+ `goal.roundsStarted` |
| `turnOutline` | 不存在 | `{ turn, prompt, response }[]`——"轮次 + 最近摘要"正来自它 |
| `title` | 对象 | `list()` 给的是**已解包的值**：字符串，或缺失时为 `null` |
| `inbox` | 未知 | `{ 'next-turn': [], 'next-step': [] }` |

### 7. 空闲会话是**冷的**

真机观测：一个对端被唤醒、跑完一轮、回了信，随后**又变回 `running: false`**；`peer_list` 列出的 17 个会话全是 `not running`。

所以"每次投递都确认一次唤醒"在实践中等于**每条消息两张卡片**——那不是安全，是摩擦。**定稿行为是一张卡覆盖整个决定**（§7）：卡片正文在需要唤醒时明说这一点，通道批准涵盖唤醒，**撤销通道就是关闭开关**。

### 8. `once` 档的语义是**一次往来**，不是一条消息

"仅本次投递"批准后额度归零、通道作废，于是**对端的回复需要一次新的批准**——而且卡片出现在**对端会话**里，人看到的是"我不是刚批准过吗"。

定稿语义：**一条消息 + 它对某条 request 的回复**。实现见 `store.noteRequest` / `isAnswerableOn`。方向校验是必需的：**只认由对端发起**的 request，否则一方可以拿自己发过的 request 去解锁另一方的额度。

### 9. `turnOutline` 的预览是**逐字摘录**，不是抽象摘要（刻意如此）

每轮最多 200 字符、取最近 3 轮，所以一次进度查询最多带出约 600 字符**原文**。真机看过实际输出之后，用户选择**保留 200 字符**。

**不要当漏洞"修"掉**——它是一个记录在案的选择。要改就改 `core.js` 的 `RECENT_TURNS` / `PREVIEW_MAX_CHARS` 两个常量，并与用户确认。

### 10. 粘贴文本可能带**零宽字符**

零宽空格、word joiner、bidi 控制符：`\s` 不匹配、`trim()` 去不掉、**渲染时不可见**。真机上表现为自相矛盾的报错——`未知子命令「connect」`，同时把 `connect` 列为可用。

`stripInvisible()` 在解析命令前和目标匹配前各调用一次（**后者同样重要**：粘贴带零宽字符的**标题**会匹配不到明明存在的会话）。`displayToken()` 让报错打出码位。

### 11. `followup` 会让入站消息出现在**对端的输入区**（刻意保留）

`followup` 的契约是"排一个独立回合并唤醒驱动"，所以入站消息会以**排队用户回合**的形式出现在客户端输入区，看起来像收件人自己待发送的内容。

这是**选定行为**：当前原语下"能被唤醒"与"不进输入区"互斥（`inject` 靠**不唤醒**换取不进输入区）。从安全角度它反而是优点——一条来自别的会话的消息**在执行前可见、可否决**。抬头第一行是让这份可见性保持诚实的补偿。

### 12. `silent: true` 与冷对端**互斥**

静默投递的语义是"只添加上下文、不开启回合"，而唤醒正好是它的反面。这种情况**直接拒绝**（`silent-needs-running`），连卡片都不弹——一个自相矛盾的请求不该拿去让人裁决。

### 13. Windows 侧 git 会把三个脚本显示成"已修改"——**那不是真的改动**

真机症状：在 Windows PowerShell 里 `push` 成功之后，`git status -sb` 显示

```
 M install.sh
 M scripts/link-deps.sh
 M uninstall.sh
```

**正好是那三个 `100755` 的文件。** Windows 侧 git 经 UNC/9p 读到的是 `100644`，与索引不符，于是它认定文件被改过。内容一个字节都没变——GitHub 自己的 Trees API 确认远端存的是 `100755`。

**两条规矩，第二条反直觉**：

- **不要从 Windows 侧 `add` / `commit`。** 那边提交会把 `100755 → 100644` 记进历史，三个脚本就不再可执行，`./install.sh` 会坏。Windows 侧**只用它做 `push`**。
- **不要用 `git config core.fileMode false` 去消掉那三行。** `.git/config` 是**共享**的（同一个 `.git`），那会**同时关掉 WSL 侧的检查**——而那个检查正是你发现脚本权限被改的唯一防线。**让 Windows 侧显示"脏"，是对的状态。**

这条还解释了为什么只有 3 个文件被标脏、而不是全部 23 个：`.gitattributes` 的 `eol=lf` 挡住了 CRLF 那一类噪音（见坑 12 旁边的发布一节）。

## 七、测试哲学：夹具必须对**数据**忠实

这是本项目最贵的一课，出现过三次，每次代价都不小。

初版测试的 fixture **对类型忠实、对数据不忠实**：全是对象或 `{}`，从没喂过 `null`，投影形状全凭猜测。于是下面这些**单元测试结构上抓不到**：

| 缺陷 | 真机才暴露的原因 |
|---|---|
| `title` 为 `null` 时崩溃 | fixture 里没有 `null` |
| `todos` 数组/`goal` 嵌套猜错 | fixture 用的是**我以为的**形状 |
| re-apply 静默清空通道 | 只有真进程会 re-apply |
| `once` 的回复要二次批准 | 语义冲突只在真机上显得荒谬 |
| 零宽字符导致自相矛盾的报错 | 假输入永远干净 |
| 拒绝时倾倒整份列表 | 假上下文里只有 2 个会话，不是 16 |

**两条纪律**：

1. 写 fixture 前先去**声明该类型的包里抄真实形状**，不要凭直觉。
2. 假宿主不要返回**写死的**值——让假 answerer **回显它收到的标签**，让假会话用**真实投影键名**。写死的值会在插件换一种语言/形状后静默失配，而测试还是绿的。

## 八、在真机上验证一次改动

`npm test` 只覆盖逻辑。**任何涉及真机契约的改动都要人工跑一遍**：

```sh
./install.sh          # 或 npm run link（只改代码时）
# 重启 web profile —— 必须，见坑 1
```

然后在对话里：

| 步骤 | 期望 |
|---|---|
| `/peers` | 列出通道；命令描述与结果应为客户端语言 |
| `/peer connect <标题> session` | 建通道，**不发送任何内容**（零 token） |
| `/peer revoke <标题>` | 断开；紧接着发送应**重新弹卡片** |
| 发送目标不存在 | 应**具名说明原因**（已归档 / 子代理 / 空白），而不是倾倒整份列表 |

再让一个**对端会话**验另一半：它能否看到命令、能否 `peer_progress` 读到本会话、回复能否闭环。

`docs/design.md` §12 有一张"已在真机验证 / 仍未验证"的对照表——**改动前先看它**，改完按需更新。

## 九、仓库卫生

**绝对不能提交**（`.gitignore` 已处理一部分，但人要自觉）：

- 任何**真实会话标题**、真实**工作目录**、真实**用户名**或主机名
- 凭据、token、cookie、`settings.yaml`、`~/.dsh` 下任何东西
- 本机绝对路径（跨平台示例请用 `/srv/...` 这类中性路径）
- 私有的 `MAINTENANCE-NOTES.md`（与公开的 `MAINTENANCE.md` 是两份不同的东西）

改完文档后建议扫一遍。**用你自己机器上的标记做模式**——用户名、挂载前缀、邮箱域——下面只是形状示例，故意用了不会误伤任何人的占位值：

```sh
git ls-files -z | xargs -0 grep -In -E 'REPLACE_WITH_YOUR_USERNAME|REPLACE_WITH_YOUR_MOUNT|@example\.com' || echo clean
```

**还要专门扫"已被匿名化的会话"有没有以字面形式泄漏**——上面那条通用扫描抓不到它，因为真实标题本身不是你的用户名也不是邮箱：

```sh
# 用你**实际见过**的会话标题与 session id 做模式（不要把它们写进本文件！）
git ls-files -z | xargs -0 grep -In -E '<某个真实标题>|<某个 session id>'
# 发布后对每个 tag 再扫一次：写进文档的泄漏要趁早发现
for tag in $(git tag); do git grep -qE '<真实标题>|<session id>' "$tag" -- . && echo "LEAK in $tag"; done
```

上面**故意不写实例**：一旦把真实标题当作"要搜的模式"写进公开文档，它本身就成了一次泄漏——这正是本文件里那个反面教材的成因。

```sh
git ls-files -z | xargs -0 grep -In -E 'REPLACE_WITH_YOUR_USERNAME|REPLACE_WITH_YOUR_MOUNT|@example\.com' || echo clean
```

上面这行**本身就是这条规则的示范**：写文档时最容易犯的错，就是把真实路径和用户名当成"要搜的模式"写进公开文档。

**关于语言**：`docs/design.md` 与 `MAINTENANCE.md` 是中文（主要读者的语言），README 是双语的，**代码与代码注释是英文**。新增文案要同时补 `lib/i18n.js` 的 `zh` 和 `en` 两张表——测试里有一条会检查两张表的键完整性，漏键会让界面出现裸标识符。

## 十、发布（推送）

本仓库的推送路径**取决于机器**，不是通用的。一般形状：

- 若 WSL 里 `git push` 不可靠、而 Windows 侧有可用代理：在 **Windows PowerShell** 里设好 `HTTP(S)_PROXY`，然后经 **UNC 路径**操作**同一个 `.git`** 推送。因为操作的是同一个 `.git`，`origin/main` 会立即同步，**不需要在 WSL 里 `fetch`**。
- 认证走 Git Credential Manager 的浏览器登录，**不需要 PAT**。
- **具体端口、UNC 路径、账号名不进本文件**——它们在本目录下一份被 gitignore 的 `MAINTENANCE-NOTES.md` 里（存在就读它）。这份公开文档绝不能承载机器相关的标识。

**推送后务必在 WSL 侧核对两件事**：

```sh
git status --short              # 必须为空
git ls-files -s install.sh      # 必须是 100755
```

Windows 侧显示的"已修改"是视错觉——见坑 13。

## 十一、当前状态与下一步（M2 交接）

**M1 已完成并在真机上逐条验证**。`docs/design.md` §12 的对照表是"验过什么"的唯一记录——**开工前先看它**。

### M2 的任务，按建议顺序

| # | 任务 | 状态 / 为什么排在这里 |
|---|---|---|
| 1 | **`peer_inbox` 能按 id 取回消息正文** | **已完成。** 原本是**唯一一项"有肉眼可见缺陷"的**：inbox 只显示 `summary`（一行），正文只能从转录里那条 relay 消息读到——**消息一旦不在上下文里，拿着 id 也取不回来**。 |
| 2 | inbox 摘要里补 `replyTo` | **已完成**，与 1 同批。 |
| 3 | 收件箱**已读推进** | **已完成**（语义由用户在看过真机列表后选定：**取回正文才算读**）。实现时暴露并修掉一个潜伏缺陷——见下面"顺手抓到的真缺陷"。 |
| 4 | 环路防护完整版 | **已完成**（2026-09-14）：通道级速率窗口 30/分、跳数上限 3、`replyWithin` 首次被解析成真正的 `dueAt` 并到点通知一次。参数与理由见 `docs/design.md` **§11.5**；真机复核待做。 |
| 5 | 进度渲染补 `plan` / `contextPressure` | **决定不做**（用户裁定，理由见 `docs/design.md` §13）：C1 的清单**已全部渲染**，这两个键都不在其中；且未渲染的投影里有一半（`subagent*` 等）**本就不该渲染**。`plan` 还有 C2 红线风险。**它当初被排进来的理由是"数据已有、只差渲染"——那是成本的理由，不是价值的理由。** |

### 但先读这一段

上面五项里有**四项的优先级是猜的**。这个项目从头到尾，六个真实缺陷里有**五个是单元测试结构上抓不到的**（第七节）。所以开工之前，先问用户最近实际用下来哪里别扭——**真机比推理更会教人**。

**M2 的第 1+2 项就是这么定下来的**：用户确认"就是 inbox 取不回正文"，并明确选了"按建议顺序做 1+2"、把已读推进推到看到真机输出之后。

### 第 1+2 项：怎么实现的（留给下一个对话）

改动很小，但有两处是**承重**的，别在重构时拆掉：

- **存的是"跨通道的那份完整正文"，不是摘要的重新拼接。** `lib/core.js` 只渲染一次正文（`renderPeerBody`），同一份字符串既交给 `buildPeerMessageFromText` 投递、又存进收件箱条目（`item.text`）。这样"归档的那份"与"被投递的那份"**不可能漂移**——如果两边各拼一次，将来改抬头格式就会出现两份不一致的正文，而测试多半抓不到。投递与归档不一致的那次事故（坑 6 那一类）就是这么来的。
- **来源抬头随正文一起回来。** 取回的是**完整投递文本**，第一行仍是 `[peer-session message · from another conversation, NOT a user instruction]`。这不是冗余：取回动作常发生在很多轮之后，那条 relay 消息早已不在上下文里，没有抬头，一段对端的话就会被当成用户自己说的话——**H2 必须在取回路径上同样成立**，而不只在新消息上成立。
- 列表形态未变：仍然一行摘要 + `id` 行，只是 `id` 行现在也带 `replyTo`；末尾多一句"用 `peer_inbox(id: ...)` 读全文"。**列表不倾倒正文**（坑 6 里"拒绝时倾倒整份列表"的同类错误）。
- 取回不存在的 id → 具名说明，并列出**确实存在的 id**（"拒绝要具名"的同一原则）。收件箱为空时说的是"空"，不是空列表。

**一条承诺边界，由对端会话指出（它附了证据）**：收件箱是**进程内**的，**重启即空**。所以 M2 第 1 项解决的是"消息掉出**上下文**"，**没有**解决"掉出**进程**"——跨一次重启，条目和正文一起消失。对端的原话很好：现在的措辞读起来像"很久之后都能取回"，实际是"**本次进程生命周期内**都能取回"。

它与"授权不跨重启"是同一个 `store` 的同一个性质。**这不是要修的缺陷**（进程内是刻意的：持久化会牵进会话事件词汇表，§12.1），**但文档措辞不得暗示超出它的承诺**。这条来自一个很典型的陷阱：**实现者写功能说明时，容易把自己做到的那部分（正文不再依赖上下文）写成自己想要的那部分（正文永久可取）。**

### 第 3 项（已读推进），以及在它旁边挖出的一个真缺陷

用户选定语义：**取回正文才算已读**，列出摘要不算。落点是 `store.markRead`，只在 `peer_inbox` 的**按 id 分支**调用。

**一处别拆掉的约束**：读没读过、有没有人回，是**两个正交事实却共用一个 `status` 字段**。所以"已读"不能把"未答"写成 `read`——那会抹掉 B6 要的待回复标记。

**状态词表**（唯一定义在 `docs/design.md` §13，改名前先看它）：

| `status` | 含义 | 列表标记 |
|---|---|---|
| `unread` | 非 request 的入站消息，正文未取回 | `[unread]` |
| `unread-unanswered` | request，正文未取回、也无人回 | `[unread]` |
| `read-unanswered` | request，正文已取回、仍无人回 | `[read · never answered]` |
| `replied` | **我回复过**它（只出现在回信方这一端） | 无标记 |

两个真机教我的坑，都在这一项里：

- **标签不能与状态矛盾**：`read-unanswered` 最初叫 `new`，被对端会话当场指出——"一个刚读过的条目显示 `new`，读起来像未读"。它说得对。
- **改名必须连标签表一起改**：改名时漏改了 `readMarker`/`stateLabel`，新状态名直接落到兜底值，表现是**刚收到的 request 第一次列出时完全没有标记**。现已由护栏测试钉住：每个状态都要有自己的标签，不许落到兜底。

#### 真缺陷：`markReplied` 只查了通道的一端

待回复条目记录在**请求接收方**的收件箱里（`deliver` 写 `to: peerId`）。问题是**谁持有它，取决于这轮交换是谁先开口的**，而两个方向都合法：

| 场景 | 谁发 request | 待回复条目落在谁那里 | 谁回信 | 标记必须写进 |
|---|---|---|---|---|
| A 收到请求后回信 | B | **A** | A | A（`other(channel, A)`） |
| A 自己开的头,收到 B 的回信 | A | **B** | B | B（B 自己的） |

旧实现固定查**答方自己**的收件箱，于是第一行那种场景（**收到别人的请求再回信**，也就是最常见的用法）**永远标不上**：回复照常送达、`replyTo` 照常串联，只有"已回复"这个标记静默落空。

**修法是"两端都查"**（`[sessionId, other(channel, sessionId)]`），而不是去推断谁先开口——通道本来就握着两端是谁，不需要关于"谁先说话"的假设。`markReplied` 的第三个参数是通道。

#### 我在诊断这件事上翻了三次，这才是最该读的部分

1. 我先写了个断言，说"**发起方**的收件箱里那条请求应该变成已回复"。它失败，我据此宣布"发现缺陷"。
2. 随后我又从代码"读出"方向相反，宣布**我自己错了、原实现是对的**，把修复回滚。（这一步还引用了 `test/peer.test.js` 里 `markReplied('b', 'req-1')` 那条老测试作为"证据"——但那条测试只覆盖**一端**，它跟这个方向问题无关。）
3. 全绿之后，另一条断言**仍然失败**，我才终于**去测**：给 `markReplied` 打临时探针，打印 `inboxKeys` 与每条 `requestId/status`。数据一秒定论——请求在答方的对端，旧实现查错了。

**为什么第 2 步那么危险**：我把"测试通过"当成了"我对了"，而没有意识到自己是**把断言改成迁就了新实现**。测试跟着实现改，就不再是测试。识别信号很直接：**如果我需要修改断言才能让测试通过，先问这个断言原先在断言什么语义。**

**另一条同源教训**：同一批断言里还有 `assert.match(settled, /adapted/)`，而 `adapted` 只是回复自己 summary 里的词——**标记完全失效也照样通过**。松断言不会报错，它只会制造"这条路径已被覆盖"的幻觉。现在改成 `store.findInboxItem(SELF, requestMessageId).status === 'replied'`，直接钉住**这条路径事后留下的状态**，而不是本次调用的返回文本。

**留下的护栏**：`lib/store.js` 的 `markReplied` 注释写明了"两个方向都合法、别只查一端"；测试断言的是**具体那条请求条目**的状态（用 `findInboxItem` 按 id 取，不用 `[0]`——收件箱最新在前，`[0]` 是回复本身）。

#### 同一种错误也会从对端来：证据有效、推断合理、结论错误

这次协作里，对端会话也做了一次同形的误判，而且**它的证据是真的**：它看到同一类条目"早上显示 `[replied]`、此刻什么都不显示"，于是判断我们丢了一个标签。这个推断在它掌握的信息下完全合理——**但它错了**，因为"已回答 → 无标记"本来就是设计（`replied` 只出现在回信方那一端，所以带标记的那一端永远看不到它）。它随后**主动、明确地收回了**判断。

两点值得留下：

- **协作者不是权威，标签不是结论。** 它给我的三条观察里，两条（已读推进的前后对照、`[never opened]` 与无标记并列）**质量很高、直接可用**；第三条是错的。**逐条核，不要整包接受或整包否定。**
- **"已回复 → 静默"是设计，不是缺陷。别给它加标记。** 想在答方看到"已回复"，正确的做法是在自己的收件箱里看**自己发出去的 request**（那条属于对方，你看到的是你自己收到的消息）。这条同时说明为什么 `replied` 永远不出现在"被你回复的那条"上。

那位对端随后**自己核对并撤回了**那两条字面"出入"——它跑的是改名前的构建（`new` 正是 `34fc2fd` 改掉的旧名）。它给出的判据比"先对版本"更可用，值得照抄：

> **比对行为（标记变没变）不受版本影响；比对字面（标记写成哪个词）必须同版本。**

同一份列表里唯一有效的那条证据——已回信条目**无标记**——是**行为**，所以它成立；两条字面差异都来自版本错配，所以它们无效。跨会话核对时先做这个区分，能省掉一整轮往返。

### 第 4 项（环路防护）：参数在哪、以及测试它的两个坑

**参数都集中在 `lib/store.js` 顶部**，改之前先读 `docs/design.md` §11.5（那里写了每道防护"防什么、不防什么"）：`CHANNEL_BUDGET`(200 总量) · `RATE_LIMIT`/`RATE_WINDOW_MS`(30 条/60 秒) · `HOP_LIMIT`(3 跳) · `HOP_MEMORY_MS`(15 分钟)。

设计上三处容易改错：

- **跳数由插件记，不写进消息让模型带**。模型漏传一次，断掉的是**防护**。收发的两侧都在 store 里记（投递时 `noteInboundHop`，发送时 `nextHop`），模型看不到也不需要这个数字。
- **跳数只防级联，不防"聊太久"**。A→B→C→A 这种接力才受它限制；两个会话正常来回很多轮由速率和额度管。所以它必须配一个**记忆过期时间**，否则二十分钟前的一条入站消息会莫名其妙把现在这条算成第 4 跳。
- **超时通知是被动的、只报一次**（`reported` 标记）。插件无法在对方会话里主动开一轮，所以通知搭在这个会话**下一次动作**上；`peer_list` 另外列出"还在等谁回复"，不必轮询。

**写测试时踩的两个坑，都值得记住——它们不是实现 bug，是测试本身在骗人：**

1. **默认 locale 是中文，我却断言英文文案。** `new PeerCore(ctx)` 直接用默认 locale（中文），传 `locale: { preference: 'en' }` 也**不生效**（那是插件入口 `index.js` 解析的，不是 core）。表现是断言 `Overdue: 1 request` 对着中文输出失败。**教训**：文案断言要么显式绑定 locale，要么写成**与语言无关**的形式（匹配 `req-1`、数字、占位符是否被插值）。错误消息的匹配同理——用 `/hop|跳/` 而不是 `/hop/`。
2. **速率窗口是"通道级"的，而 `once` 档用完就撤销重建通道。** 于是每条投递都拿到**全新的窗口**，`rateCount` 永远停在 1，速率上限**根本测不到**。第一版测试正是这样"通过"了一半。改用 `session` 档让通道持续存在，才真正压到上限。**教训**：测一条"按对象记账"的上限之前，先确认那个对象**在你的测试里活得够久**。

顺带一个语义细节：窗口内**被拒绝的尝试仍然计数**（`withinRate` 先计数再判断）。这是有意的——否则连拒几次之后窗口还没过就又能发了。

**真机第一轮就抓到一个单元测试覆盖不到的边界**：`peer_inbox` 在**空收件箱**时提前 `return 'Peer inbox is empty.'`，而那一步跳过了"询问是否有超时请求"——**询问即消费**（`reported` 标记），于是那次唯一的超时通知被一个丢弃它的分支吃掉了。空收件箱恰恰是等待者最会去翻的时候。修法：把超时请求的收集**提到任何提前返回之前**。

这条又一次印证第七节：**"状态被读取时就顺便被消费"这种设计，最容易被提前返回绕过，而单元测试的 fixture 里通常没有"空 + 有待报事项"这个组合。** 现在测试钉住了它（空收件箱也必须报出超时请求、且下次不再重复）。

另外记一个**有意的取舍**：`peer_list` **不消费**超时通知。它是只读视图，且已用 `OVERDUE` 标注同一事实；在那里也弹通知会把同一件事说两遍。

### 一次改动的标准流程

1. `npm run link` → `npm test`（79 项）
2. 任何涉及真机契约的改动 → **必须真机验一遍**：`./install.sh` + **重启 profile**（坑 1）
3. 改了文案 → 同时补 `lib/i18n.js` 的 `zh` 与 `en`（有测试检查两张表的键完整性）
4. 改了行为 → 更新 `docs/design.md` §12 的验证表
5. 踩到新坑 → **回写本文件第六节。这份文档的价值全在这里。**
6. 提交前 → 跑第九节的敏感信息扫描

## 十一之一、宿主升级：为什么"能跑"不等于"接对了"

**症状**：把 DSH 从 `0.1.5-rc.3` 升到 `0.1.7-rc.2` 后，插件**照常工作**（`peer_list` 正常列出会话、地址解析正确）。但一比对就发现：插件加载的是 **0.1.5-rc.3** 的模块，宿主跑的是 **0.1.7-rc.2**。

**根因**：`~/.dsh/profiles/node_modules` 里的 `@deepseek-ai/*` 链到**全局安装**那份（`~/.npm-global/.../dsh/node_modules`），而 npx 启动的宿主用的是**它自己缓存**里那份。两者版本不同，`link-deps.sh` 只认前者，于是**插件的 node_modules 指向了旧实例**。

**这为什么危险**：正是坑 3 警告的身份分裂——`defineTool`、`createUserMessage` 这些"定义类型的包"在插件与宿主手里**是两份**。它此刻不报错（0.1.7 恰好向后兼容），但那是运气，不是保证；一旦哪版改了返回形状或符号，插件就会以旧形状对新宿主。

**修法与教训**：
- `scripts/link-deps.sh` 改为**优先从 PATH 上的 `dsh` 反推宿主实例**（`readlink -f $(command -v dsh)` → 向上找含 `@deepseek-ai/dsh-tools` 的 `node_modules`），`$DSH_HOME/profiles/node_modules` 只作兜底。现在脚本会打印它选了哪个实例、以及该实例的 `dsh-llm` 版本。
- **第一版修法仍不对，而且错得很典型**：它只在每一层找 `$d/node_modules`。这只覆盖 **npx 缓存**布局（`…/_npx/<hash>/node_modules/.bin/dsh`，包就在上一层）；而**全局安装**把 dsh 的依赖**打包在包内部**——`…/lib/node_modules/@deepseek-ai/dsh/node_modules/` ——外层 `node_modules` 里根本没有 `@deepseek-ai/dsh-tools`。于是脚本"找到路径但判定不可用"，静默回退到 `profiles/node_modules`，**看起来成功、其实没选到宿主那份**。现在每层同时检查 `$d/node_modules` 与 `$d/*/node_modules`。**教训**：写"向上搜索"这类逻辑，要拿**实际存在的每种布局**各验一遍；只用你手上那一种布局测过，就等于没测。
- **升级宿主后必须重跑 `link-deps.sh`（或 `./install.sh`）并重启 profile**，否则插件可能仍挂在旧实例上。
- **"插件能跑"不能证明"接对了"**：这次功能全正常，错的是模块身份。**判据要直接看 `readlink -f` 与包版本，不要看行为是否正常。**

### 0.1.7-rc.2 的契约变化（实测逐项比对）

宿主升级要查的不是"有没有报错"，而是**插件依赖的每个契约**。这次逐项比对结果：

| 契约 | 变化 | 影响 |
|---|---|---|
| `dsh-llm` / `dsh-tools` **导出符号集合** | **完全一致** | 无 |
| `sessionController.resolveAgent(sessionId)` | 签名完全相同 | 无 |
| `sessionController.list(request, signal)` | 签名完全相同 | 无 |
| `SessionListValue.items` | 仍是 `SessionSummary[]` | 无 |
| `SessionSummary` | **只新增 `agentAvailable: boolean`**（向后兼容）；插件读的 `sessionId`/`running`/`blank`/`origin`/`cwd`/`updatedAt`/`projections` 全在 | 无 |
| `userQuestions` | 导出符号无差异 | 无 |
| `dsh-api-session-controller` 其他 | 移除 `SessionJob`/`SessionQueuedItem`，新增 `ArchivedSessionGate`/`SessionProjectionsValue` 等 | 插件未使用，无影响 |

**顺带发现的可用增强**：新字段 `SessionSummary.agentAvailable` 是宿主的权威信号——"这个会话当前是否持有活 Agent"。插件现在是用 `ctx.agents.get(peerId)` 判活的（坑 7：空闲会话是冷的），**两者语义不同**：`agentAvailable` 反映**权威状态**，`agents.get` 反映**本进程的即时视图**。插件用在"要不要唤醒、要不要再校验"上，当前实现（重读 `agents.get`）是对的，**不要顺手改成 `agentAvailable`**——那会把一个并发正确的判断换成一个可能滞后的快照。这里记下来，是为了让下一个人知道"多了一个字段"不等于"该用它"。

**验证方法**（可复用）：拿新实例**真实跑一遍插件的注册路径**，比读类型定义更硬。做法是建一个探针目录，`node_modules` 软链到新实例、插件目录软链进去，然后 `registerTools` + 参数编译 + `createUserMessage` 全过一遍。本次三项全 PASS。

## 十一之二、v1.0.0 发布前的审计：它抓到了什么，以及过程本身的教训

第一版正式版之前做了一次全量审计：**两个独立子代理各审一半**（生命周期/状态寿命/并发 vs 边界/不可信输入/错误路径），各自用探针驱真实代码取证。**20 项发现、11 项真 bug，全部已修**，测试 70 → 79。逐项记录在 `docs/design.md` 的「v1.0.0 审计」一节。

**为什么值得分两个审查者**：他们说"独立"，价值就在这里——**消息 id 碰撞被两人各自独立发现**，而**方向校验缺失**只有边界那位发现、**H1 在 await 后被打破**只有生命周期那位发现。同一个脑子会沿同一路径漏掉同一批东西。

### 审计过程本身教了我三件事（比那 11 个 bug 更通用）

1. **两个审查者都说"仓库脏了、我没动"，那是我的改动。** 怀疑报告先核对事实，别先怀疑工具。
2. **`python str.replace` 静默失效会毁掉一整轮工作。** 我用脚本改 `noteReply` 加方向校验，字符串没匹配上、脚本不报错、我以为改好了——**是那条新测试把它抓出来的**。此后凡脚本改代码，**先 `assert count == 1` 再替换**。这是本次最危险的一次操作，因为它伪装成"已完成"。
3. **同文件里的同类断言可能沿用了同一个错误假设。** 有两条测试都用 `inboxFor(X)[0]` 取条目，而收件箱是**最新在前**，`[0]` 常常是"那条回复"而不是"那条请求"——它们此前"通过"是因为碰巧。**断言要按 id 取（`findInboxItem`），不要依赖数组位置。** 同理，一条测试把"请求方向"写反后仍能通过，是新加的方向校验把它照出来的。

### 发布前该做的（照抄）

1. `npm test` 全绿（79 项）。
2. 两个独立审查（一个人审不出自己写的路径）。
3. 逐条修 → **每条修复都补回归测试**（没有测试的修复是下一次重构的牺牲品）。
4. 敏感信息扫描（第九节）。
5. 版本号与 `files` 白名单核对：`npm pack --dry-run` 看哪些文件真的会进包——**README 引用的 `docs/`、用户要跑的 `install.sh` 都曾在白名单外**。
6. 打 tag、写发布说明；推送走第十节（本机必须 Windows 侧）。

## 十一之三、v1.0.1：一次"规格写对了、实现违反了规格"的故障

发布后**一个对端会话**递来一份故障报告（**它的真实标题不写在这里**——见下）：那个会话同时与三个会话协作，**中途被跳数上限永久锁死**，最后只能退回文件通道。报告质量很高，逐条都成立。

> **这份文档自己差点违反第九节。** 我最初把那**真实会话标题**写进了上面这句话——批评报告文件含标题的同时，自己在公开文档里犯了同一个错。**发现方式**：发布后用 `git grep -E '<真实标题>|session-<id>' <tag>` 扫已发布的 tag，它会当场命中。**已改成描述性说法（"一个对端会话"）。以后引用任何会话只写角色，不写标题。**

### 事实

`HOP_LIMIT = 3` 的**实现**是：只要「最近 15 分钟内收到过对端消息」，本次发送就 `+1`，**不看收件人**。于是**同一对会话普普通通的四次往返还**（请求→回复→澄清→再回复）就走完 1、2、3、4，此后**该对永久无法再发**：无重置手段、授权不能扩容、`peer_list` 还显示 `remaining=193`（那是**另一个轴**——消息配额）。

### 要害：规格是对的，实现违反了它

`docs/design.md` 第 256 行原本写着：

> **跳数上限只防级联，不防"聊太久"**：它拦的是 A→B→C→A 这类**一条链被接力转发**；两个会话正常来回聊很多轮**不受它限制**。

**规格写对了。** 我实现成了"每次往返 +1"。所以这次修正**没有改规格，而是让实现回到规格上**——正确的判据是「这条消息给了谁」：回复刚给你写信的那个会话 → **跳数不变**；发给第三方 → **+1**。

**这类 bug 特别危险，因为它长在"两层文档都正确"的阴影里**：规格说了、`MAINTENANCE` 也说了，只有代码没照做。读文档的人会以为没问题，读代码的人（我）以为自己按文档写了。**唯一能抓住它的是"用真实场景走一遍"**，而报告方正好提供了那个场景。

### 报告里另外三条也都成立，一并修了

1. **`peer_list` 不暴露跳数轴**，只给 `remaining`，于是跳数拒绝看起来像配额不足——**用户据此让我"补充连接次数"，双方各浪费一轮**。现在两个轴都显示并各自带单位：`remaining=193 (messages) · hop=1/3 (relay depth, 2 left)`。
2. **错误文案不给出路**，只写"不允许无限接力转发"，读起来像"换个发法就行"。现在附一段解释：管的是**接力深度**、**回复不算**、**没有任何重置手段**（换类型/新开/再要授权都不行）、以及真正可行的替代（**请用户代为转达**、共享工作区就用文件）。
3. **`summary` 必填从没写明**（schema 里 `required: true` 是对的，但描述只说"可选详情"在 body 上），有人省略后收到宿主的 `missing required property` 而插件根本没运行。描述现在以 **Required.** 开头。

### 教训（比这次修复更通用）

- **上限要按"行为语义"定义并测，不要按"看起来相关的信号"近似。** 我用"最近收到过消息"近似"这件事在接力"，而这两者根本不等价。
- **多轴限制必须一起显示。** 只显示一个轴，另一个轴的拒绝就会被误诊成前者的不足——用户和 agent 都会在错误的方向上花掉轮次。
- **拒绝文案要给"出路"，并且明确排除死路。** 说"不允许无限接力"是不够的；要让读者知道**换发法没用、要授权没用**，以及**什么才有用**。
- **对端会话的报告值得当真逐条核。** 这份报告 6 条全部成立，其中 P0 两条是我自己在真机上也没遇到的（我从没用完过跳数）。

## 十二、进一步阅读

| 想了解 | 去哪 |
|---|---|
| 为什么这么设计、每条需求落在哪个原语上 | `docs/design.md`（**权威**） |
| 模型要怎么用这个插件 | `skill/peer-session/SKILL.md` |
| 真机上验过什么、怎么验的 | `docs/design.md` §12 |
| 安装与文件布局 | `README.zh.md` |
